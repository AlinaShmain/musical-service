import { ToTimeFormatPipe } from './to-time-format.pipe';

describe('ToTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ToTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
